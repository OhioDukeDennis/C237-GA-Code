<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Event Management App</title>
  <style>
    body {
      font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
      background-color: #F5F5DC;
      /* beige background color */
    }

    .navbar-brand {
      font-size: 24px;
      text-align: left;
      width: 100%;
      color: #34C759;
    }

    .navbar {
      background-color: #000;
      /* Make the navbar black */
    }
  </style>
</head>

<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Event Management App</a>
      <a class="nav-link" href="/event" style="color: #34C759;">Add new event</a>
    </div>
  </nav>

  <div class="container">
    <br>
    <div class="text-center">
      <h2>Event Information</h2>
    </div>
    <br>
    <% if (event) { %> <!-- Check if event data exists -->
      <div>
        <p><b>Title:</b><br>
          <%= event.title %>
        </p>
        <p><b>Venue:</b><br>
          <%= event.venue %>
        </p>
        <p><b>Venue Image:</b><br> <img src="/images/<%=event.image %>" width="20%"></p>
        </p>
        <p><b>Date:</b><br>
          <%= new Date(event.date).toDateString() %>
        </p>
        <p><b>Start Time to End Time:</b><br>
          <%= new Date(event.startTime).toLocaleTimeString('en-US', { hour: '2-digit' , minute: '2-digit' , hour12: true
            }) %>
            - <%= new Date(event.endTime).toLocaleTimeString('en-US', { hour: '2-digit' , minute: '2-digit' , hour12:
              true }) %>
        </p> <!-- Display start time and end time -->
        <p><b>Description:</b><br>
          <%= event.description %>
        </p>
        <p><b>Capacity:</b></br>
          <%= event.capacity %>
        </p>
        <p><b>Registration Deadline:</b><br>
          <%= new Date(event.regDeadline).toLocaleDateString() %> at <%= new
              Date(event.regDeadline).toLocaleTimeString('en-US', { hour: '2-digit' , minute: '2-digit' , hour12: true
              }) %>
        </p>
        <p><b>Status:</b></br>
          <%= event.status %>
        </p>
      </div>
      <% } else { %>
        <p>No event found.</p>
        <% } %>
          <a href="/index">Back</a>
  </div>

</body>

</html>